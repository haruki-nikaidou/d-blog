---
import RootLayout from '@/layouts/RootLayout.astro';
import siteConfig from '../../../content/site.json';
import { getCollection } from 'astro:content';
import Card from "../../components/Card.astro";
import TagList from "../../components/TagList.astro";

const SITE_TITLE = siteConfig.title;
const SITE_DESCRIPTION = siteConfig.description;

const posts = (await getCollection('blog')).filter((post) => !post.data.draft);
const tags = Array.from(new Set(posts.flatMap((post) => post.data.tags))).sort();
---

<RootLayout title={`タグ | ${SITE_TITLE}`} description={SITE_DESCRIPTION} robots="noindex,follow">
  <Card class="tags-card">
    <h2>タグ</h2>
    <TagList tags={tags} />
  </Card>
</RootLayout>

<style>
  main {
    max-width: 70rem;
    margin: 3rem auto;
    padding: 0 1rem;
  }

  .tags-card {
      padding: 1rem;
      width: 100%;
  }
</style>
